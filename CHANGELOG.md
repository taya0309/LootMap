# 更新记录（CHANGELOG）

**当前版本：v0.18.5**

> 使用前请先看 [README 的「运行前提」](README.md#运行前提重要先看这里)：
> 星形标记与新版面板（F7）都画在 **RuffnecKk MapSense** 的叠加层里，
> **必须装了 MapSense 才有星、F7 才是新版面板**；没装的话只有物品颜色生效，
> F7 弹出的是插件自带的老式原生面板。

---

## v0.18.5（2026-09-23）—— 少装东西也能一眼看出原因

- 别人（或你换机器）**没装 MapSense** 时，面板会退回老式原生面板；
  以前日志里只有一句「native panel」，看不出根因。
  现在会明写一行 `panel fallback ... mapsense_module=0` 说明「缺 MapSense」，
  并说明**没有它就没有星标**。
- 配置文件 `loot-map.toml` 顶部也加了这段运行前提说明。

## v0.18.4（2026-09-22 23:59）—— 星默认大小 16；位置微调滑条回归

- **星默认半径改为 16**（滑条范围仍是 12~36）。
- 星星区加回 **「左右微调」/「上下微调」**（±50 像素）：星的落点和分辨率、
  界面缩放有关，拖一下对准物品即可，**自动保存**，以后每次进游戏都是对准的。

## v0.18.3 —— 面板滚轮真正修好

- 真因：这个 ImGui 版本的滚轮是在**每帧开头**统一处理的，之前我们是在帧中间写
  IO 字段 —— 当帧早已处理完、下一帧开头又被清零，等于白写。
- 改走官方**事件队列**接口后，面板内容超出窗口时可以用滚轮滚了。

## v0.18.2 —— 滚轮诊断版（内测）

- 判定改用与「点击」相同的、不受 DPI 缩放影响的标志；并加一次性日志定位卡点。

## v0.18.1 —— 面板滚轮（第一版）

- 新增低级鼠标钩子线程：光标在面板上时把滚轮喂给面板，同时阻止游戏跟着滚。

## v0.18.0 —— 字体统一（★ 已实测通过）

- 在**游戏第一次构建字体库之前**把微软雅黑注册进宿主字体图集（飘字插件同款路数），
  面板中文、数字、标点从此**同一字型**，不再"一行字两种字体"。
- 三重保险：字库已建好就跳过 / 全程异常防护 / `own_font = false` 可关闭。

## v0.17.7 / v0.17.6 —— 字体实验的"爆炸半径归零"

- v0.17.6 试图自己给宿主渲染器做字体纹理，猜内部结构时踩到坏指针，
  导致**面板和星标一起消失**（游戏本身没事）。
- v0.17.7 起：所有可疑指针先用探针试、整段流程套防护罩、并留保命开关 ——
  **最坏情况只是回到"可读但字体混排"，不会再拖累面板和星标**。

## v0.17.0 – v0.17.5 —— 面板照 MapSense 重做

- **面板重做**：总开关 + 可折叠分区（品质 / 规则 / 星星），窗口更小；
  品质只放「暗金 / 套装」，其余内部保留、不占用界面。
- 每行颜色 = 14 色预设色板 **＋ 完整调色板**（任意颜色）。
- 区块改为**上下排列**（勾选一行、色板独占一行），不再截断。
- 调色板弹窗不再被面板压住。

## v0.16.0 —— 「珠宝匠 / 工匠」蓝装规则星上线

- 带这两个前缀的 **4 孔蓝装**会亮一颗独立的规则星（默认蓝色），
  与「蓝色(魔法)」开关互相独立。
- 判据是**前缀编号**（1209/1210，实测于 当时实测所用模组 模组），因此**不会误认「工匠之」**。
- 换模组后编号可能不同，用 `tools/diff_starid.py` 对样本重新校准即可。

## v0.15.0 – v0.15.2 —— 星标外观（暗黑3 风）

- 星标按暗黑3 地图金星重做：**边缘深、中心亮的分层渐变** + 圆润星臂 + 深色描边；
  面板上可随时切回「简约」平板样式。
- 修掉"引擎自己画的小绿星 + 灰底块"漏出来的问题（关键：只认"换了另一件物品"
  才松开拦截，怪物取色不许解开）。

## v0.14.0 – v0.14.9 —— 星标稳定性（一堆毛病逐个根治）

- **面板重做**：八类品质一视同仁、每类可选颜色；面板矩形内的星半透明透出
  （以前"调参数星就消失"就是把面板底下的星整片藏掉了）。
- **"满天星"根治**：
  - 引擎对**同一件物品**一帧会反复取色几十次 → 改成**按物品身份去重**：一件一颗星。
  - 「引擎色号兜底」把"色号 0（引擎表示没给颜色）"误当暗金色号 → 满地图杂货全被判成
    星品。已把该兜底**默认关闭、且 0 永不算颜色**。
  - 「星标驻留」会把星**钉在屏幕上跟着人物跑** → 默认改为 0（只画引擎当帧报的）。
  - 面板状态不再影响星的亮度（开背包不再忽明忽暗）。

## v0.12.8 —— 早期"正常版"（用户认可的稳定基线）

- 星位置公式定案；面板去掉颜色控件。
- 这一版长期作为出问题时的回退版本（`archive\v0.12.8\`）。

## v0.2 – v0.12 —— 从零到能用

- 摸清地图标记的绘制入口 → 改物品颜色 → 换标记形状 → 借 MapSense 叠加层画自定义图标
  → 星形标记成型 → 设置面板（F7）→ 面板挡鼠标（点面板时人物不动）。

---

## 已知问题 / 取舍

| 项目 | 说明 |
|---|---|
| 星标与新版面板依赖 MapSense | 见 README「运行前提」；没装只有颜色、没有星 |
| 蓝装前缀编号 | 1209/1210 实测于 当时实测所用模组 模组；换模组需用 `tools/diff_starid.py` 重校准 |
| 星的落点 | 与分辨率/界面缩放有关，用面板「左右/上下微调」对准一次即可 |
| 「星标驻留」 | 调大能让星多显示几秒，但那几秒位置会飘（引擎不再报时坐标不再跟世界走）；建议保持 0 |
| 手改 `loot-map.toml` | 面板上一动任何控件就会按内存值重写该文件；要改请在面板上改 |
| 游戏大版本更新 | 内存偏移可能变化，需要重新校准 |
